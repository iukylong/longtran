<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kait.longlongtran.storyvoz">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.DetailActivityOffline"
            android:label="@string/title_activity_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.ReadStoryActivityOffline"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OffLineActivity" />
        <activity
            android:name=".UI.OnlineActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginFirebase.LoginActivity" />
        <activity android:name=".UI.DetailActivityOnline" />
        <activity android:name=".UI.ReadStoryActivityOnline"></activity>
    </application>

</manifest>